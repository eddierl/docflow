node_modules
dist

.terraform*
*.tfstate
*.tfstate.backup

.env

.turbo

test-results

*.traineddata

# Turbo Docker prune artifacts (created when running `pnpm turbo prune --docker` on the host)
out
pnpm-lock.yaml.turbo-pruned

# Terraform lambda packaging artifact (built by pnpm build)
terraform/lambda-upload.zip
